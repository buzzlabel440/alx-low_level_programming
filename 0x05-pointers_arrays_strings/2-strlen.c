#include "main.h"
#include <string.h>
/**
 * _strlen -> functin to get the length of a string
 * @s: string pointer to passed to this function
 * Return: returns length of a string
 */
int _strlen(char *s)
{
	return (strlen(s));
}
